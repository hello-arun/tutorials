#####################################################
Requirements for /**BANDS**/ module are

must:
    scf file
    bands file

optional:
    kPath in base.sh
    see tools/Bands.py to tune the plot options
#####################################################